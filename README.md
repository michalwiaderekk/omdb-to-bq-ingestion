# omdb-to-bq-ingestion
This repository contains a Google Cloud Function that retrives movie data from the OMDB API based on a provided movie title, and ingests the resulting JSON into a BigQuery table.
